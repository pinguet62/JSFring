databaseChangeLog:
-   changeSet:
        id: 03-encrypt-password
        author: Pinguet62
        comment: Encrypt password
        changes:
        -   sql:
                sql: UPDATE "user" SET password = CONCAT('{noop}', password)
        rollback:
        -   sql:
                sql: update "user" set password = SUBSTRING(password, 7)

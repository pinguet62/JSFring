Use **Node.js** server:
* `cd ./src/main/webapp/`
* `npm install`
* `npm start`
* 

Use **Spring-Boot**:
* `mvn clean package`
* `java -jar target/webapp-angular2-1.0-SNAPSHOT.war`
or
* `mvn spring-boot:run`